<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar</title>
</head>
<body>
    <h1>Editar</h1>

    <form action="/task/edit/{{task._id}}" method="post" >
        <div> 
            <label for="title">Elemento</label>
            <input type="text" name="title" placeholder="¿Que va a prestar?" id="title" value="{{task.title}}">
        </div>

        <div>
            <label for="description">description</label>
            <textarea name="description" placeholder="description" id="description" cols="30" rows="10">{{task.description}}</textarea>
        </div>    

        <div>
            <label for="code">Codigo o Cédula</label>
            <input type="text" name="code" placeholder="Codigo o Cédula" id="code" value="{{task.code}}">
        </div>

        <div> 
            <label for="lugar">lugar</label>
            <input type="text" name="lugar" placeholder="lugar de uso" id="lugar" value="{{task.lugar}}">
        </div>

        <div> 
            <label for="lugar">Tel</label>
            <input type="text" name="telefono" placeholder="numero de contacto" id="telefono" value="{{task.telefono}}">
        </div>

        <div> 
            <label for="fecha">Fecha</label>
            <input type="text" name="fecha" placeholder="fecha" id="fecha" value="{{task.fecha}}">
        </div>

        <div> 
            <label for="hora">Hora</label>
            <input type="text" name="hora" placeholder="hora" id="hora" value="{{task.hora}}">
        </div>

        <button type="submit">Editar</button>



</body>
</html>